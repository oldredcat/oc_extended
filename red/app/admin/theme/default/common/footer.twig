{% if styles %}
    {% for style in styles %}
        <link rel="{{ style.rel }}" media="{{ style.media }}" href="{{ style.href }}">
    {% endfor %}
{% endif %}
  {% if scripts %}
      {% for script in scripts %}
          <script src="{{ script }}"></script>
      {% endfor %}
  {% endif %}
</html>
